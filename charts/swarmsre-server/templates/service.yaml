apiVersion: v1
kind: Service
metadata:
  name: {{ include "swarmsre-server.fullname" . }}
  labels:
    {{- include "swarmsre-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swarmsre-server.selectorLabels" . | nindent 4 }}
